# Design System

## 1. Concept

**深夜证物实验室：灰白扫描物证悬浮在低照度工作台上，暗红校验章标记真正重要的状态。**

这是产品界面，不是品牌落地页。气氛来自材质、层级、留白和精确反馈，而不是霓虹特效。视觉优先级始终是：物证几何 → 当前工具和异常信号 → 档案文字 → 环境装饰。

## 2. Color Strategy

采用 Restrained 策略。背景为近黑纯中性；品牌主色沿 `353°` 暗红，用于当前选择、封缄、关键确认和错误；冷灰蓝用于普通扫描状态；琥珀只用于警告。

```css
:root {
  --color-bg: oklch(0.105 0 0);
  --color-surface: oklch(0.155 0.006 260);
  --color-surface-raised: oklch(0.205 0.008 260);
  --color-ink: oklch(0.93 0.006 260);
  --color-muted: oklch(0.68 0.012 260);
  --color-primary: oklch(0.56 0.17 353.2);
  --color-primary-strong: oklch(0.48 0.19 353.2);
  --color-accent: oklch(0.72 0.055 235);
  --color-warning: oklch(0.76 0.12 78);
  --color-success: oklch(0.72 0.10 152);
  --color-danger: oklch(0.63 0.19 25);
  --color-border: oklch(0.31 0.009 260);
  --color-focus: oklch(0.82 0.08 235);
}
```

规则：主色占可见界面的比例不超过 10%；大面积背景不带品牌红；白光、侧光、紫外和线框工具用形状与文字共同区分，不能只换颜色。

## 3. Typography

- UI 主字体：`"IBM Plex Sans SC", "Noto Sans SC", system-ui, sans-serif`。
- 数字与编号：`"IBM Plex Mono", "SFMono-Regular", Consolas, monospace`。
- 不使用独立展示字体；产品任务优先于品牌戏剧性。
- 正文 16px/1.65；紧凑标签 12～13px；页面标题 32～40px；工作台标题 20～24px。
- 正文行长控制在 65～75ch；档案正文可略窄。
- 编号使用 `font-variant-numeric: tabular-nums`。

## 4. Spacing and Geometry

采用 4px 基础单位：`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64`。

- 常规控件高度 40px，紧凑工具按钮 36px，关键主操作 44px。
- 圆角克制：控件 4px，主要面板 8px；不用胶囊形按钮作为默认。
- 边框 1px；不使用彩色粗侧边框。
- 案件入口使用不对称 12 栏布局；调查工作台采用左侧物证主视区、右侧信息轨道、底部工具栏。
- 避免卡片套卡片。分区优先用留白、细线、背景层级和标题建立关系。

## 5. Component Vocabulary

### Buttons

- Primary：暗红实底、近白文字，仅用于开始调查、确认线索、提交结案。
- Secondary：石墨表面、完整细边框，用于切换和次要动作。
- Ghost：透明背景，用于工具栏和返回操作。
- 所有按钮包含 default / hover / focus / active / disabled / loading。

### Evidence Status

- 未检查：轮廓图标 + “未检查”。
- 调查中：扫描弧形标志 + 进度文字。
- 核心完整：实心校验章 + “关键线索完整”。

状态不只依赖颜色。

### Panels

- 工作面板使用低对比实色，不使用玻璃模糊。
- 标题与内容通过间距分组，不把每段内容包成独立卡片。
- 错误和提示优先内联展开；只有不可逆结案提交可使用确认对话框。

### Loading

- 案件页使用内容骨架。
- 3D 物证加载以“扫描切片”进度表现，但进度值必须真实来自加载器。
- 禁止只在中央放置无限旋转圆圈。

## 6. Motion

- 常规状态变化 160～220ms，使用 `cubic-bezier(0.22, 1, 0.36, 1)`。
- 物证切换允许 280ms 的短暂曝光/扫描过渡。
- 线索发现以局部信号收束、档案条目写入表示，不撒彩纸、不弹跳。
- 页面内容默认可见，动效只增强状态变化。
- `prefers-reduced-motion: reduce` 下取消相机补间、扫描闪烁和位移动画，仅保留瞬时或淡入反馈。

## 7. Accessibility

- 正文对背景至少 4.5:1；关键操作和焦点必须清晰。
- `:focus-visible` 使用双层轮廓或 2px 高对比描边，不依赖阴影微光。
- 键盘顺序遵循视觉顺序；工具栏使用 roving tabindex 或标准按钮组。
- 所有图标按钮都有可见或辅助技术可读标签。
- 允许 150% 文本缩放；信息面板滚动，但关键主操作保持可达。
- 错误、成功、工具选中与热点信号均同时使用图形和文字。

## 8. Responsive Behavior

桌面为首发目标：

- `>= 1180px`：物证视区 + 右侧信息轨道并列。
- `800～1179px`：右侧信息轨道收为可展开面板，工具栏保持底部。
- `< 800px`：显示可浏览的简化布局和设备建议；不隐藏档案，但第一版不承诺完整精细调查体验。

响应式变化是结构重排，不缩小所有文字和按钮。
